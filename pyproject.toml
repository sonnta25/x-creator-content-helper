[project]
name = "x-telegram-content-bot"
version = "0.1.0"
description = "Telegram bot that generates X posts/replies from live trends and tweet links."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27,<1",
    "python-dotenv>=1.0,<2",
    "python-telegram-bot>=21.8,<23",
    "twscrape>=0.19.2,<1",
    "yt-dlp[default]>=2025.1,<2027",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
